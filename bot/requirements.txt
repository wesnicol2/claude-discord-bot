# Claude Discord Bot – Python dependencies
# Pin minor versions for reproducible builds; patch versions float for security fixes.
discord.py>=2.3,<3.0
PyNaCl>=1.5,<2.0      # Suppresses discord.py "voice will NOT be supported" warning
